[dev-packages]
flake8 = ">=3.5.0"
pytest = ">=4.1.0"
pytest-django = ">=3.4.5"
pytest-sugar = ">=0.9.2"

[packages]
django = "<=2.1.6"
PyMySQL = ">=0.9.3"
gunicorn = "<=19.9"
django-webpack-loader = "<=0.6.0"

[scripts]
# tests = "bash ./run-tests.sh"

[pipenv]
#allow_prereleases = true
